"""create a function that will display the how many items in the list have names more tan 5 characters

names=["utkarsh","ankit","ramendra","adhiraj"]"""

def count(list):
    total=0
    for i in list:
        if len(1)>5:
            total=total+1
        else:
            total=total
        return total
list=[]
total=count(list)
print(total)